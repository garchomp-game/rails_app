<%= form_with(model: authro, local: true) do |form| %>
  <% if authro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(authro.errors.count, "error") %> prohibited this authro from being saved:</h2>

      <ul>
      <% authro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :authro_user_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :authro_name %>
  </div>

  <div class="field">
    <%= form.label :birth %>
    <%= form.date_select :birth, id: :authro_birth %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address, id: :authro_address %>
  </div>

  <div class="field">
    <%= form.label :ctype %>
    <%= form.text_field :ctype, id: :authro_ctype %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.text_field :photo, id: :authro_photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
